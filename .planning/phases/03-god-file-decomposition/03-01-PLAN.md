---
phase: 03-god-file-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/storage.ts
  - server/storage/types.ts
  - server/storage/alerts.ts
  - server/storage/incidents.ts
  - server/storage/connectors.ts
  - server/storage/organizations.ts
  - server/storage/auth.ts
  - server/storage/playbooks.ts
  - server/storage/compliance.ts
  - server/storage/ai.ts
  - server/storage/ioc.ts
  - server/storage/cspm.ts
  - server/storage/dashboard.ts
  - server/storage/predictive.ts
  - server/storage/jobs.ts
  - server/storage/response-actions.ts
  - server/storage/reports.ts
  - server/storage/billing.ts
  - server/storage/war-room.ts
  - server/storage/evidence.ts
  - server/storage/audit.ts
  - server/storage/notifications.ts
  - server/storage/misc.ts
  - server/storage/index.ts
autonomous: true
requirements:
  - SPLIT-03

must_haves:
  truths:
    - "import { storage } from './storage' resolves correctly in all 53+ consumer files"
    - "import type { IStorage } from './storage' resolves correctly"
    - "TypeScript compiles with zero errors (npx tsc --noEmit)"
    - "All existing tests pass without modification"
    - "No single domain module file exceeds 800 lines"
  artifacts:
    - path: "server/storage/types.ts"
      provides: "IStorage interface (~895 lines)"
      contains: "export interface IStorage"
    - path: "server/storage/index.ts"
      provides: "DatabaseStorage class + storage singleton + barrel re-exports"
      exports: ["storage", "IStorage", "DatabaseStorage"]
    - path: "server/storage/alerts.ts"
      provides: "Alert domain standalone functions"
      contains: "export async function getAlerts"
    - path: "server/storage/incidents.ts"
      provides: "Incident domain standalone functions"
      contains: "export async function getIncidents"
  key_links:
    - from: "server/storage/index.ts"
      to: "server/storage/alerts.ts"
      via: "import * as alertFns"
      pattern: "import \\* as alertFns"
    - from: "server/storage/index.ts"
      to: "server/storage/types.ts"
      via: "export type { IStorage }"
      pattern: "export type.*IStorage"
    - from: "any consumer file"
      to: "server/storage/index.ts"
      via: "import { storage } from '../storage'"
      pattern: "from.*[./]storage"
---

<objective>
Decompose server/storage.ts (6,243 lines) into ~18 domain modules within a server/storage/ directory, with a barrel index.ts preserving all existing import paths.

Purpose: The 6,243-line storage.ts is the largest file in the codebase and blocks maintainability. Splitting into domain modules makes each domain navigable and sets up Phase 4+ for targeted changes.

Output: server/storage/ directory with types.ts (IStorage interface), ~18 domain modules (standalone functions), index.ts (DatabaseStorage class delegating to domain modules + barrel exports), and the original storage.ts deleted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-god-file-decomposition/03-RESEARCH.md

@server/storage.ts
@server/db.ts
@server/storage/cold-query.ts
@server/storage/tiering-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract IStorage interface to types.ts and create all domain module files with standalone functions</name>
  <files>
    server/storage/types.ts
    server/storage/alerts.ts
    server/storage/incidents.ts
    server/storage/connectors.ts
    server/storage/organizations.ts
    server/storage/auth.ts
    server/storage/playbooks.ts
    server/storage/compliance.ts
    server/storage/ai.ts
    server/storage/ioc.ts
    server/storage/cspm.ts
    server/storage/dashboard.ts
    server/storage/predictive.ts
    server/storage/jobs.ts
    server/storage/response-actions.ts
    server/storage/reports.ts
    server/storage/billing.ts
    server/storage/war-room.ts
    server/storage/evidence.ts
    server/storage/audit.ts
    server/storage/notifications.ts
    server/storage/misc.ts
  </files>
  <read_first>
    server/storage.ts (FULL FILE -- read in chunks: lines 1-387 for imports, lines 388-1282 for IStorage interface, lines 1283-6243 for DatabaseStorage class)
    server/db.ts (db import pattern)
    server/storage/cold-query.ts (existing file -- do NOT modify)
    server/storage/tiering-manager.ts (existing file -- do NOT modify)
  </read_first>
  <action>
    **Step 1: Create types.ts**
    Extract the `IStorage` interface (lines 388-1282 of storage.ts) into `server/storage/types.ts`. Copy ALL the import types needed by the interface from the top of storage.ts. The file should:
    - Import only types from `@shared/schema` (use `import type`)
    - Export `interface IStorage` with all ~280 method signatures exactly as they appear in storage.ts
    - NO implementation code, only the interface

    **Step 2: Create domain module files**
    For each domain, extract the corresponding methods from the `DatabaseStorage` class (lines 1283-6242) as standalone exported async functions. Each function receives `db` implicitly (import from `../db`) and uses Drizzle operators directly. Copy the exact function body from the class method.

    Domain groupings (method prefixes/table associations):

    **alerts.ts** (~220 lines): getAlerts, getAlert, createAlert, updateAlert, deleteAlert, searchAlerts, getAlertsPaginated, getAlertStats, getAlertDailyStats, createAlertDailyStats, getAlertsByTimeRange, getDedupClusters, createDedupCluster, updateDedupCluster, getActiveDedupCluster, getAlertTags, addAlertTag, removeAlertTag, getAlertEntities, bulkCreateAlerts, getAlertArchives

    **incidents.ts** (~225 lines): getIncidents, getIncident, createIncident, updateIncident, deleteIncident, getIncidentComments, createIncidentComment, getIncidentSlaPolicies, createIncidentSlaPolicy, updateIncidentSlaPolicy, deleteIncidentSlaPolicy, getPostIncidentReviews, createPostIncidentReview, updatePostIncidentReview, getIncidentTags, addIncidentTag, removeIncidentTag, getPirActionItems, createPirActionItem, updatePirActionItem, deletePirActionItem, getIncidentResponseApprovals

    **connectors.ts** (~250 lines): getConnectors, getConnector, createConnector, updateConnector, deleteConnector, getConnectorJobRuns, createConnectorJobRun, updateConnectorJobRun, getConnectorHealthChecks, createConnectorHealthCheck, getConnectorMetrics, getConnectorSecretRotations, createConnectorSecretRotation, updateConnectorSecretRotation

    **organizations.ts** (~320 lines): getOrganizations, getOrganization, createOrganization, updateOrganization, deleteOrganization, getOrganizationMemberships, createOrganizationMembership, updateOrganizationMembership, deleteOrganizationMembership, getOrgInvitations, createOrgInvitation, updateOrgInvitation, deleteOrgInvitation, getOrgSecurityPolicies, createOrgSecurityPolicy, updateOrgSecurityPolicy, getOrgDomainVerifications, createOrgDomainVerification, updateOrgDomainVerification, getOrgSsoConfigs, createOrgSsoConfig, updateOrgSsoConfig, getOrgScimConfigs, createOrgScimConfig, updateOrgScimConfig, getOrgPlanLimits, createOrgPlanLimit, updateOrgPlanLimit, getOnboardingProgress, createOnboardingProgress, updateOnboardingProgress, getWorkspaceTemplates, getWizardProgress, createWizardProgress, updateWizardProgress

    **auth.ts** (~111 lines): getApiKeys, getApiKey, createApiKey, updateApiKey, deleteApiKey, getApiKeyByHash, getUserSessions, createSession, deleteSession, getPasswordResetTokens, createPasswordResetToken, deletePasswordResetToken, validatePasswordResetToken, getIdempotencyKey, createIdempotencyKey, updateIdempotencyKey

    **playbooks.ts** (~208 lines): getPlaybooks, getPlaybook, createPlaybook, updatePlaybook, deletePlaybook, getPlaybookExecutions, getPlaybookExecution, createPlaybookExecution, updatePlaybookExecution, getPlaybookApprovals, createPlaybookApproval, updatePlaybookApproval, getPlaybookVersions, getPlaybookVersion, createPlaybookVersion, updatePlaybookVersion, getBlastRadiusPreviews, createBlastRadiusPreview, getPlaybookSimulations, createPlaybookSimulation, updatePlaybookSimulation, getPlaybookRollbackPlans, createPlaybookRollbackPlan, updatePlaybookRollbackPlan

    **compliance.ts** (~300 lines): getCompliancePolicies, getCompliancePolicy, createCompliancePolicy, updateCompliancePolicy, deleteCompliancePolicy, getComplianceControls, getComplianceControl, createComplianceControl, updateComplianceControl, deleteComplianceControl, getComplianceControlMappings, createComplianceControlMapping, deleteComplianceControlMapping, getPolicyChecks, createPolicyCheck, getPolicyResults, createPolicyResult, getDsarRequests, getDsarRequest, createDsarRequest, updateDsarRequest, getEvidenceLockerItems, createEvidenceLockerItem, updateEvidenceLockerItem, deleteEvidenceLockerItem, getLegalHolds, createLegalHold, updateLegalHold

    **ai.ts** (~102 lines): getAiFeedback, getAiFeedbackByResource, createAiFeedback, updateAiFeedback, getAiDeploymentConfigs, getAiDeploymentConfig, createAiDeploymentConfig, updateAiDeploymentConfig, getAiChatHistory, createAiChatMessage, getGeneratedRules, createGeneratedRule

    **ioc.ts** (~163 lines): getIocFeeds, getIocFeed, createIocFeed, updateIocFeed, deleteIocFeed, getIocEntries, getIocEntry, createIocEntry, updateIocEntry, deleteIocEntry, getIocWatchlists, getIocWatchlist, createIocWatchlist, updateIocWatchlist, deleteIocWatchlist, getIocWatchlistEntries, createIocWatchlistEntry, deleteIocWatchlistEntry, getIocMatchRules, createIocMatchRule, updateIocMatchRule, deleteIocMatchRule, getIocMatches, createIocMatch

    **cspm.ts** (~151 lines): getCspmAccounts, getCspmAccount, createCspmAccount, updateCspmAccount, deleteCspmAccount, getCspmScans, createCspmScan, updateCspmScan, getCspmFindings, getCspmFinding, createCspmFinding, updateCspmFinding, getCspmDriftBaselines, createCspmDriftBaseline, getCspmDriftEvents, createCspmDriftEvent, getCspmDspmFindings, createCspmDspmFinding, getCspmAttackPaths, createCspmAttackPath, getCspmRemediations, createCspmRemediation, updateCspmRemediation

    **dashboard.ts** (~232 lines): getDashboardStats, getDashboardAnalytics, getDashboardMetricsCache, updateDashboardMetricsCache

    **predictive.ts** (~172 lines): getPredictiveAnomalies, createPredictiveAnomaly, updatePredictiveAnomaly, getAttackSurfaceAssets, createAttackSurfaceAsset, updateAttackSurfaceAsset, getRiskForecasts, createRiskForecast, getAnomalySubscriptions, createAnomalySubscription, updateAnomalySubscription, deleteAnomalySubscription, getForecastQualitySnapshots, createForecastQualitySnapshot, getHardeningRecommendations, createHardeningRecommendation, updateHardeningRecommendation

    **jobs.ts** (~296 lines): getJobs, getJob, createJob, updateJob, claimNextJob, getOutboxEvents, createOutboxEvent, updateOutboxEvent, getSliMetrics, createSliMetric, getSloTargets, createSloTarget, updateSloTarget, getDrRunbooks, createDrRunbook, getDrDrillResults, createDrDrillResult, getTicketSyncJobs, createTicketSyncJob, updateTicketSyncJob, plus any SLO-related helpers

    **response-actions.ts** (~200 lines): getResponseActions, getResponseAction, createResponseAction, updateResponseAction, getResponseActionRollbacks, createResponseActionRollback, getResponseActionApprovals, createResponseActionApproval, updateResponseActionApproval, getInvestigationRuns, getInvestigationRun, createInvestigationRun, updateInvestigationRun, getInvestigationSteps, createInvestigationStep, updateInvestigationStep

    **reports.ts** (~120 lines): getReportTemplates, getReportTemplate, createReportTemplate, updateReportTemplate, deleteReportTemplate, getReportSchedules, createReportSchedule, updateReportSchedule, deleteReportSchedule, getReportRuns, createReportRun, updateReportRun, getReportTemplateVersions, createReportTemplateVersion

    **billing.ts** (~141 lines): All billing/subscription/invoice/usage-metering related methods, getUsageMeterSnapshots, createUsageMeterSnapshot

    **war-room.ts** (~125 lines): All war room related methods (rooms, participants, messages, action items, handoffs)

    **evidence.ts** (~129 lines): getEvidenceItems, createEvidenceItem, updateEvidenceItem, deleteEvidenceItem, getInvestigationHypotheses, createInvestigationHypothesis, updateInvestigationHypothesis, getInvestigationTasks, createInvestigationTask, updateInvestigationTask, getEvidenceChainEntries, createEvidenceChainEntry, getEvidenceAttachments, createEvidenceAttachment

    **audit.ts** (~107 lines): getAuditLogs, getAuditLog, createAuditLog, getAuditLogCount, getAuditLogsByResource, getAuditLogsByUser, getAuditLogsByAction

    **notifications.ts** (~105 lines): getNotificationChannels, createNotificationChannel, updateNotificationChannel, deleteNotificationChannel, getNotificationPreferences, createNotificationPreferences, updateNotificationPreferences, getNotificationDeliveryLogs, createNotificationDeliveryLog

    **misc.ts** (~500 lines): Everything not in the above domains: tags (getTags, createTag, etc.), integrations, webhooks, webhook logs, saved views, feature flags, suppression rules, auto-response policies, runbook templates, runbook steps, endpoint assets, endpoint telemetry, posture scores, threat intel configs, alert archives, entities/alertEntities

    **CRITICAL RULES for each domain module:**
    1. Import `db` from `"../db"` (NOT from `"../../db"` -- the file is in server/storage/)
    2. Import Drizzle operators from `"drizzle-orm"` directly
    3. Import table schemas and types from `"@shared/schema"`
    4. Do NOT import from sibling domain modules (no circular deps)
    5. Export each function as a named export
    6. Copy function bodies EXACTLY from DatabaseStorage class methods -- no behavioral changes
    7. Remove `this.` references, replacing with direct `db` usage
    8. Keep the same function signatures (same params, same return types)
  </action>
  <verify>
    <automated>cd "C:/Users/prath/OneDrive/Desktop/securenexus/.claude/worktrees/brave-vaughan" && ls server/storage/types.ts server/storage/alerts.ts server/storage/incidents.ts server/storage/connectors.ts server/storage/organizations.ts server/storage/auth.ts server/storage/playbooks.ts server/storage/compliance.ts server/storage/ai.ts server/storage/ioc.ts server/storage/cspm.ts server/storage/dashboard.ts server/storage/predictive.ts server/storage/jobs.ts server/storage/response-actions.ts server/storage/reports.ts server/storage/billing.ts server/storage/war-room.ts server/storage/evidence.ts server/storage/audit.ts server/storage/notifications.ts server/storage/misc.ts 2>&1 && wc -l server/storage/*.ts | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - server/storage/types.ts exists and contains `export interface IStorage`
    - All 18+ domain module files exist in server/storage/
    - Each domain module imports db from "../db" (grep -c 'from "../db"' server/storage/alerts.ts returns >= 1)
    - No domain module imports from a sibling module (grep -r 'from "./' server/storage/*.ts should only show "../db" and "./types" patterns, no cross-domain imports)
    - No single domain module exceeds 800 lines (wc -l server/storage/*.ts shows all under 800)
  </acceptance_criteria>
  <done>All domain modules created with standalone functions extracted from DatabaseStorage class. IStorage interface in types.ts. No file exceeds 800 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Create barrel index.ts with DatabaseStorage delegation, delete original storage.ts, verify compilation and tests</name>
  <files>
    server/storage/index.ts
    server/storage.ts (DELETE)
  </files>
  <read_first>
    server/storage.ts (lines 1283-1400 to see DatabaseStorage class opening and first methods for delegation pattern)
    server/storage/types.ts (just created -- verify IStorage is exported)
    server/storage/alerts.ts (just created -- verify export names match)
  </read_first>
  <action>
    **Step 1: Create server/storage/index.ts**

    The barrel file must:
    1. Import all domain modules as namespace imports: `import * as alertFns from "./alerts"`, etc.
    2. Import `IStorage` from `"./types"` and re-export it: `export type { IStorage } from "./types"`
    3. Define `DatabaseStorage` class implementing `IStorage`
    4. For each method in `IStorage`, assign the corresponding domain function as a class property:
       ```typescript
       export class DatabaseStorage implements IStorage {
         // Alerts
         getAlerts = alertFns.getAlerts;
         getAlert = alertFns.getAlert;
         createAlert = alertFns.createAlert;
         // ... every single method
       }
       ```
    5. If TypeScript complains about property assignment pattern (type inference issues), fall back to one-line delegation wrappers:
       ```typescript
       async getAlerts(orgId?: string) { return alertFns.getAlerts(orgId); }
       ```
    6. Export the singleton: `export const storage = new DatabaseStorage()`
    7. Do NOT re-export cold-query.ts or tiering-manager.ts (they have their own import paths)

    **Step 2: Delete server/storage.ts**

    CRITICAL: The file `server/storage.ts` must be deleted so bundler resolution finds `server/storage/index.ts` instead. If both exist, esbuild picks the .ts file over the directory, breaking the split.

    Use `git rm server/storage.ts` to delete and stage in one step.

    **Step 3: Verify compilation**

    Run `npx tsc --noEmit` -- must produce zero errors. If there are errors:
    - Missing method in DatabaseStorage: add the delegation
    - Type mismatch: check the domain module function signature matches IStorage
    - Import resolution: ensure no consumer file changed (they should all resolve to storage/index.ts now)

    **Step 4: Verify tests**

    Run `npx vitest run --reporter=verbose` -- all existing tests must pass without modification. No consumer file should need changes since `import { storage } from "./storage"` now resolves to `./storage/index.ts`.

    **Step 5: Verify no file exceeds 800 lines**

    Run `wc -l server/storage/*.ts` and confirm every file is under 800 lines. The index.ts will be the largest (one line per method delegation + imports), likely ~600-700 lines.
  </action>
  <verify>
    <automated>cd "C:/Users/prath/OneDrive/Desktop/securenexus/.claude/worktrees/brave-vaughan" && npx tsc --noEmit 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/storage.ts does NOT exist (test -f server/storage.ts returns false)
    - server/storage/index.ts exists and contains `export const storage`
    - server/storage/index.ts exports IStorage type (grep 'export type.*IStorage' server/storage/index.ts)
    - server/storage/index.ts exports DatabaseStorage class (grep 'export class DatabaseStorage' server/storage/index.ts)
    - npx tsc --noEmit exits with code 0 (zero TypeScript errors)
    - npx vitest run exits with code 0 (all tests pass)
    - No file in server/storage/ exceeds 800 lines (wc -l server/storage/*.ts | awk '{if($1>800 && $2!="total") print "FAIL:",$2}' returns empty)
  </acceptance_criteria>
  <done>storage.ts is fully decomposed. Original file deleted. All 53+ consumer imports resolve to storage/index.ts. TypeScript compiles cleanly. All tests pass. No file exceeds 800 lines.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero errors (all imports resolve, all types match)
2. `npx vitest run --reporter=verbose` -- all tests pass
3. `test ! -f server/storage.ts` -- original file deleted
4. `test -f server/storage/index.ts` -- barrel exists
5. `wc -l server/storage/*.ts` -- no file over 800 lines
6. `grep -r "from.*['\"]\.\.\/storage['\"]" server/routes/ | head -5` -- consumer imports unchanged
</verification>

<success_criteria>
- server/storage.ts replaced by server/storage/ directory with 18+ domain modules
- Barrel index.ts preserves storage, IStorage, DatabaseStorage exports
- Zero TypeScript compilation errors
- All existing tests pass without modification
- No single file exceeds 800 lines
- Existing cold-query.ts and tiering-manager.ts untouched
</success_criteria>

<output>
After completion, create `.planning/phases/03-god-file-decomposition/03-01-SUMMARY.md`
</output>
