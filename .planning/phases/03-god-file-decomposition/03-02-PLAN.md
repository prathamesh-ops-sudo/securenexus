---
phase: 03-god-file-decomposition
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - server/routes/ai.ts
  - server/routes/ai/index.ts
  - server/routes/ai/setup.ts
  - server/routes/ai/triage.ts
  - server/routes/ai/narrative.ts
  - server/routes/ai/feedback.ts
  - server/routes/ai/prompts.ts
  - server/routes/ai/active-learning.ts
  - server/routes/ai/deployment.ts
  - server/routes/ai/investigation.ts
  - server/routes/ai/context.ts
  - server/routes/ai/models.ts
  - server/routes/ai/detection-rules.ts
  - server/routes/ai/helpers.ts
  - server/routes/index.ts
autonomous: true
requirements:
  - SPLIT-01

must_haves:
  truths:
    - "All AI API endpoints respond identically after the split (same paths, same behavior)"
    - "The event listener for system.ai_circuit_open fires exactly once on route registration"
    - "TypeScript compiles with zero errors"
    - "All existing tests pass without modification"
    - "No single file in server/routes/ai/ exceeds 800 lines"
  artifacts:
    - path: "server/routes/ai/index.ts"
      provides: "Barrel registerAiRoutes that delegates to sub-modules"
      exports: ["registerAiRoutes"]
      contains: "export function registerAiRoutes"
    - path: "server/routes/ai/triage.ts"
      provides: "Triage and correlation apply endpoints"
      contains: "registerAiTriageRoutes"
    - path: "server/routes/ai/narrative.ts"
      provides: "Narrative generation and streaming endpoints"
      contains: "registerAiNarrativeRoutes"
    - path: "server/routes/ai/investigation.ts"
      provides: "Deep investigation, multi-turn, attack graphs, chat"
      contains: "registerAiInvestigationRoutes"
  key_links:
    - from: "server/routes/index.ts"
      to: "server/routes/ai/index.ts"
      via: "import { registerAiRoutes } from './ai'"
      pattern: "registerAiRoutes"
    - from: "server/routes/ai/index.ts"
      to: "server/routes/ai/triage.ts"
      via: "registerAiTriageRoutes(app)"
      pattern: "registerAiTriageRoutes"
---

<objective>
Decompose server/routes/ai.ts (3,544 lines, 70+ endpoints) into ~12 domain modules within a server/routes/ai/ directory, with a barrel index.ts that preserves the registerAiRoutes export.

Purpose: The AI routes file is the second-largest in the codebase. Splitting by domain (triage, narrative, investigation, etc.) makes each concern navigable and enables targeted changes in later phases.

Output: server/routes/ai/ directory with domain modules, barrel index.ts, and the original ai.ts deleted. The import in server/routes/index.ts continues to work unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-god-file-decomposition/03-RESEARCH.md
@.planning/phases/03-god-file-decomposition/03-01-SUMMARY.md

@server/routes/ai.ts
@server/routes/index.ts
@server/routes/shared.ts

<interfaces>
<!-- From server/routes/index.ts: -->
import { registerAiRoutes } from "./ai";
// Called as: registerAiRoutes(app);

<!-- From server/routes/shared.ts (shared utilities used by AI routes): -->
// Provides: getOrgId, logger, p, storage, strictLimiter

<!-- From server/routes/ai.ts line 145: -->
export function registerAiRoutes(app: Express): void {
  // Event listener registered ONCE at line 147
  eventBus.on("system.ai_circuit_open", ...);
  // Then 70+ route registrations follow
}

<!-- Helper function (file-local, line 50): -->
async function persistAttackGraph(result: Record<string, unknown>, incidentId: string, orgId: string): Promise<void>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all AI route domain modules and barrel index.ts</name>
  <files>
    server/routes/ai/index.ts
    server/routes/ai/setup.ts
    server/routes/ai/triage.ts
    server/routes/ai/narrative.ts
    server/routes/ai/feedback.ts
    server/routes/ai/prompts.ts
    server/routes/ai/active-learning.ts
    server/routes/ai/deployment.ts
    server/routes/ai/investigation.ts
    server/routes/ai/context.ts
    server/routes/ai/models.ts
    server/routes/ai/detection-rules.ts
    server/routes/ai/helpers.ts
  </files>
  <read_first>
    server/routes/ai.ts (FULL FILE -- read in chunks of ~500 lines to map every endpoint to its domain module)
    server/routes/shared.ts (shared imports pattern)
    server/routes/index.ts (how registerAiRoutes is imported and called)
  </read_first>
  <action>
    **Read the full ai.ts file carefully** to identify exact line ranges for each endpoint group. Map every `app.get/post/put/patch/delete` call to a domain module.

    **Create helpers.ts** (~100 lines):
    - Move the `persistAttackGraph` function (lines 50-143) here
    - Export it as a named export for use by investigation.ts

    **Create setup.ts** (~170 lines) -- `registerAiSetupRoutes(app)`:
    Endpoints:
    - GET /api/ai/setup-status (line ~172)
    - GET /api/ai/circuit-alerts (line ~241)
    - PATCH /api/ai/circuit-alerts/:alertId/dismiss (line ~264)
    - GET /api/ai/health (line ~291)
    - GET /api/ai/config (line ~301)
    - GET /api/ai/inference-metrics (line ~305)
    - POST /api/ai/cache/clear (line ~2220)

    Imports needed: `getOrgId, logger, p, storage, strictLimiter` from `../shared`, `isAuthenticated` from `../../auth`, `resolveOrgContext, requireMinRole, requireOrgId` from `../../rbac`, `checkModelHealth, getModelConfig, getInferenceMetrics` from `../../ai`, `getCircuitBreakerStatus` from `../../ai/model-gateway`

    **Create triage.ts** (~200 lines) -- `registerAiTriageRoutes(app)`:
    Endpoints:
    - GET /api/ai/triage/settings and related triage config endpoints (line ~310+)
    - POST /api/ai/triage (line ~353)
    - POST /api/ai/triage/batch (line ~418)
    - POST /api/ai/correlate/apply (line ~809)

    Imports needed: `getOrgId, logger, p, storage` from `../shared`, `isAuthenticated` from `../../auth`, `resolveOrgContext, requireOrgId` from `../../rbac`, `triageAlert, correlateAlerts` from `../../ai`, `validateBody, bodySchemas` from `../../request-validator`

    **Create narrative.ts** (~400 lines) -- `registerAiNarrativeRoutes(app)`:
    Endpoints:
    - POST /api/ai/narrative (line ~475)
    - POST /api/ai/narrative/stream (line ~540 area)
    - GET /api/ai/narrative/... additional narrative endpoints

    Imports: same pattern as triage + `generateIncidentNarrative, streamNarrative` from `../../ai`

    **Create feedback.ts** (~350 lines) -- `registerAiFeedbackRoutes(app)`:
    Endpoints:
    - POST /api/ai/feedback (line ~863)
    - GET /api/ai/feedback/analytics (line ~2078)
    - GET /api/ai/feedback/:resourceType/:resourceId (line ~993)
    - GET /api/ai/feedback (line ~1002)
    - POST /api/ai/feedback/inline (line ~2179)
    - POST /api/ai/playbook-authoring/propose (line ~1012)
    - GET endpoints for feedback analytics (line ~977+)

    Imports: `recordFeedbackOutcome` from `../../ai/active-learning`, `validateBody, validateQuery, bodySchemas, querySchemas` from `../../request-validator`

    **Create prompts.ts** (~400 lines) -- `registerAiPromptsRoutes(app)`:
    Endpoints:
    - GET /api/ai/prompts (line ~1562)
    - GET /api/ai/prompts/:id (line ~1572)
    - GET /api/ai/prompts/:id/history (line ~1583)
    - GET /api/ai/prompts/:id/audit (line ~1593)
    - GET /api/ai/audit (line ~1603)
    - GET /api/ai/prompts/:id/ab-tests (line ~1633)
    - POST /api/ai/prompts/:id/ab-tests (line ~1643)
    - PATCH /api/ai/prompts/:id/ab-tests/:testId (line ~1679)
    - GET /api/ai/prompts/:id/variables (line ~1803)
    - GET /api/ai/prompts/categories (line ~1846)
    - GET /api/ai/prompts/:id/quality-scores (line ~1881)
    - POST /api/ai/prompts/:id/quality-scores (line ~1891)
    - POST /api/ai/prompts/:id/... (line ~1935+)

    Imports: `getPromptCatalogSummary, getAllRegisteredPrompts, getPromptAuditLog, getPromptVersionHistory, getPromptVersion` from `../../ai`

    **Create active-learning.ts** (~200 lines) -- `registerAiActiveLearningRoutes(app)`:
    Endpoints:
    - GET /api/ai/active-learning/auto-examples (line ~2024)
    - GET /api/ai/active-learning/suppression-status (line ~2046)

    **Create deployment.ts** (~300 lines) -- `registerAiDeploymentRoutes(app)`:
    Endpoints:
    - GET /api/ai-deployment/config (line ~2230)
    - PUT /api/ai-deployment/config (line ~2241)
    - POST /api/ai-deployment/... (line ~2270+)
    - Additional deployment/model gateway endpoints

    Imports: `insertAiDeploymentConfigSchema` from `@shared/schema`, `invokeModel as gatewayInvoke` from `../../ai/model-gateway`

    **Create investigation.ts** (~500 lines) -- `registerAiInvestigationRoutes(app)`:
    Endpoints:
    - POST /api/ai/investigate/deep (line ~2321 area)
    - POST /api/ai/investigate/multi-turn (line ~2373 area)
    - GET /api/ai/investigation-graphs/:incidentId (line ~2427)
    - GET /api/ai/investigation-graphs (line ~2456)
    - GET /api/ai/investigation-graphs/detail/:graphId (line ~2476)
    - DELETE /api/ai/investigation-graphs/:graphId (line ~2502)
    - POST /api/ai/investigate/chat (line ~2526)
    - POST /api/ai/investigate/stream (line ~2577)
    - GET /api/ai/investigation/chat-history (line ~2657)
    - POST /api/ai/investigate/behavioral (line ~2694)
    - GET /api/ai/investigation-history (line ~2785)
    - GET /api/ai/investigation/... (line ~2850)

    Import `persistAttackGraph` from `./helpers`. Import `conductDeepInvestigation, conductMultiTurnInvestigation, conductThreatHunt, analyzeBehavior, predictAttackPaths, streamDeepInvestigation` from `../../ai`

    **Create context.ts** (~200 lines) -- `registerAiContextRoutes(app)`:
    Endpoints:
    - POST /api/ai/context-optimization (line ~2950)
    - POST /api/ai/hallucination-check (line ~3040)

    Imports: `buildThreatIntelContext` from `../../ai`

    **Create models.ts** (~250 lines) -- `registerAiModelsRoutes(app)`:
    Endpoints:
    - GET /api/ai/models (line ~3136)
    - PUT /api/ai/models/:modelId/preference (line ~3208)
    - GET /api/ai/data-sources (line ~3246)
    - POST /api/ai/data-sources/... (line ~3352+)
    - Additional model/data source endpoints

    **Create detection-rules.ts** (~100 lines) -- `registerAiDetectionRulesRoutes(app)`:
    Endpoints:
    - GET /api/ai/generated-rules (line ~2770)

    Import `generateDetectionRules` from `../../ai`

    **Create index.ts** (~50 lines) -- the barrel:
    ```typescript
    import type { Express } from "express";
    import { eventBus, type BusEvent } from "../../event-bus";
    import { storage } from "../../storage";
    import { logger } from "../shared";
    import { registerAiSetupRoutes } from "./setup";
    import { registerAiTriageRoutes } from "./triage";
    import { registerAiNarrativeRoutes } from "./narrative";
    import { registerAiFeedbackRoutes } from "./feedback";
    import { registerAiPromptsRoutes } from "./prompts";
    import { registerAiActiveLearningRoutes } from "./active-learning";
    import { registerAiDeploymentRoutes } from "./deployment";
    import { registerAiInvestigationRoutes } from "./investigation";
    import { registerAiContextRoutes } from "./context";
    import { registerAiModelsRoutes } from "./models";
    import { registerAiDetectionRulesRoutes } from "./detection-rules";

    export function registerAiRoutes(app: Express): void {
      // Circuit breaker event listener -- registered ONCE here, not in sub-modules
      eventBus.on("system.ai_circuit_open", (event: BusEvent) => {
        // ... copy the event listener body from original ai.ts lines 147-168
      });

      registerAiSetupRoutes(app);
      registerAiTriageRoutes(app);
      registerAiNarrativeRoutes(app);
      registerAiFeedbackRoutes(app);
      registerAiPromptsRoutes(app);
      registerAiActiveLearningRoutes(app);
      registerAiDeploymentRoutes(app);
      registerAiInvestigationRoutes(app);
      registerAiContextRoutes(app);
      registerAiModelsRoutes(app);
      registerAiDetectionRulesRoutes(app);
    }
    ```

    **CRITICAL: The event listener (eventBus.on) MUST be in index.ts, NOT in a sub-module.** This ensures it registers exactly once when registerAiRoutes is called.

    **CRITICAL: Each sub-module imports from `../shared` (one level up from ai/ to routes/), `../../auth`, `../../rbac`, etc.** Verify relative path depth is correct -- files are at server/routes/ai/triage.ts, so `../shared` resolves to server/routes/shared.ts.
  </action>
  <verify>
    <automated>cd "C:/Users/prath/OneDrive/Desktop/securenexus/.claude/worktrees/brave-vaughan" && ls server/routes/ai/index.ts server/routes/ai/setup.ts server/routes/ai/triage.ts server/routes/ai/narrative.ts server/routes/ai/feedback.ts server/routes/ai/prompts.ts server/routes/ai/active-learning.ts server/routes/ai/deployment.ts server/routes/ai/investigation.ts server/routes/ai/context.ts server/routes/ai/models.ts server/routes/ai/detection-rules.ts server/routes/ai/helpers.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - All 13 files exist in server/routes/ai/
    - server/routes/ai/index.ts contains `export function registerAiRoutes`
    - server/routes/ai/index.ts contains `eventBus.on("system.ai_circuit_open"` (event listener in barrel, not sub-module)
    - Each sub-module exports a `register*Routes(app: Express)` function
    - No sub-module contains eventBus.on (grep -r 'eventBus.on' server/routes/ai/ shows only index.ts)
    - No file in server/routes/ai/ exceeds 800 lines
  </acceptance_criteria>
  <done>All AI route domain modules created with correct endpoint groupings. Event listener in barrel index.ts. Each module exports a register function.</done>
</task>

<task type="auto">
  <name>Task 2: Delete original ai.ts and verify compilation and tests</name>
  <files>
    server/routes/ai.ts (DELETE)
  </files>
  <read_first>
    server/routes/index.ts (verify import path -- should still be `from "./ai"` which resolves to `./ai/index.ts`)
    server/routes/ai/index.ts (verify it exports registerAiRoutes)
  </read_first>
  <action>
    **Step 1: Delete the original file**
    Run `git rm server/routes/ai.ts` to delete and stage.

    **Step 2: Verify the import in server/routes/index.ts**
    The line `import { registerAiRoutes } from "./ai"` must remain unchanged. With the directory `server/routes/ai/` containing `index.ts`, bundler resolution will find it. Do NOT modify server/routes/index.ts.

    **Step 3: TypeScript compilation check**
    Run `npx tsc --noEmit`. Must produce zero errors. If errors:
    - Missing import in a sub-module: fix the import
    - Wrong relative path depth: fix `../shared` vs `./shared`
    - Missing endpoint: find which endpoint was missed and add to correct module

    **Step 4: Test run**
    Run `npx vitest run --reporter=verbose`. All tests must pass.

    **Step 5: Line count verification**
    Run `wc -l server/routes/ai/*.ts`. Every file must be under 800 lines.
  </action>
  <verify>
    <automated>cd "C:/Users/prath/OneDrive/Desktop/securenexus/.claude/worktrees/brave-vaughan" && npx tsc --noEmit 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/routes/ai.ts does NOT exist (test -f server/routes/ai.ts returns false)
    - server/routes/index.ts still contains `import { registerAiRoutes } from "./ai"` (unchanged)
    - npx tsc --noEmit exits with code 0
    - npx vitest run exits with code 0
    - No file in server/routes/ai/ exceeds 800 lines
  </acceptance_criteria>
  <done>AI routes fully decomposed. Original ai.ts deleted. All endpoints preserved. TypeScript compiles. Tests pass. No file exceeds 800 lines.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero errors
2. `npx vitest run --reporter=verbose` -- all tests pass
3. `test ! -f server/routes/ai.ts` -- original file deleted
4. `test -f server/routes/ai/index.ts` -- barrel exists
5. `wc -l server/routes/ai/*.ts` -- no file over 800 lines
6. `grep -c 'eventBus.on' server/routes/ai/index.ts` -- returns 1 (event listener in barrel)
7. `grep 'registerAiRoutes' server/routes/index.ts` -- import unchanged
</verification>

<success_criteria>
- server/routes/ai.ts replaced by server/routes/ai/ directory with 12+ domain modules
- Barrel index.ts exports registerAiRoutes and registers event listener exactly once
- Zero TypeScript compilation errors
- All existing tests pass without modification
- No single file exceeds 800 lines
- server/routes/index.ts import unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-god-file-decomposition/03-02-SUMMARY.md`
</output>
