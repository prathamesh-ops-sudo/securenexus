---
phase: 01-prerequisite-safety-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/ai.ts
  - server/auth/routes.ts
  - server/hunt-engine.ts
  - server/outbox-processor.ts
  - server/routes/ai.ts
  - server/routes/shared.ts
  - server/routes/threat-hunting.ts
  - server/routes/war-room.ts
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "Zero .catch(() => {}) patterns remain in the codebase -- every catch either logs or has an explicit justification comment"
    - "When a fire-and-forget async operation fails, the failure is logged with enough context to diagnose the issue"
    - "Running grep for '.catch(() => {})' across server/ returns zero matches"
  artifacts:
    - path: "server/ai.ts"
      provides: "Logged catch handlers on persistInferenceEntry calls"
      contains: ".catch("
    - path: "server/auth/routes.ts"
      provides: "Logged catch handlers on auth fire-and-forget calls"
      contains: "log.warn"
    - path: "server/outbox-processor.ts"
      provides: "Logged catch handlers on outbox delivery"
      contains: "log.warn"
  key_links:
    - from: "all modified files"
      to: "server/logger.ts"
      via: "logger.child() import for structured logging"
      pattern: "logger\\.child\\("
---

<objective>
Fix all 22 empty `.catch(() => {})` handlers across 8 server files by replacing them with logging calls that capture the error context.

Purpose: Silent error swallowing hides failures in AI inference tracking, auth operations, outbox delivery, and usage metering. Making these visible is prerequisite for all subsequent hardening work.
Output: Every previously-silent catch handler now logs at warn level with operation context.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From server/logger.ts:
```typescript
// Create child logger per module:
const log = logger.child("module-name");
// Usage:
log.warn("message", { key: value });
log.error("message", { error: String(err) });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace empty catch handlers with logging in AI and auth files</name>
  <files>server/ai.ts, server/auth/routes.ts</files>
  <read_first>server/ai.ts, server/auth/routes.ts, server/logger.ts</read_first>
  <action>
Replace every `.catch(() => {})` pattern with a `.catch((err) => log.warn("...", { error: String(err) }))` call.

In server/ai.ts (2 occurrences):
- Line 361: `persistInferenceEntry(metrics, true, undefined, orgId).catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to persist inference entry", { error: String(err), orgId }))`
- Line 460: `persistInferenceEntry(im, true, undefined, orgId).catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to persist inference entry", { error: String(err), orgId }))`
- Ensure `const log = logger.child("ai");` exists at module top (or use existing logger variable name if one exists already)

In server/auth/routes.ts (5 occurrences):
- Line 138: `.catch(() => {})` on storage.updateUser call -- replace with `.catch((err) => log.warn("Failed to update user on login", { error: String(err), userId: user.id }))`
- Line 180: `.catch(() => {})` on storage.updateUser call -- replace with `.catch((err) => log.warn("Failed to update user profile", { error: String(err), userId: user.id }))`
- Line 364: `.catch(() => {})` on audit/tracking call -- replace with `.catch((err) => log.warn("Failed to record login audit", { error: String(err) }))`
- Line 369: `clearLockout(user.email).catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to clear lockout", { error: String(err), email: user.email }))`
- Ensure `const log = logger.child("auth-routes");` exists at module top (check for existing logger import first)
  </action>
  <verify>
    <automated>cd "C:/Users/prath/OneDrive/Desktop/securenexus/.claude/worktrees/brave-vaughan" && grep -c ".catch(() => {})" server/ai.ts server/auth/routes.ts | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
- `grep ".catch(() => {})" server/ai.ts` returns 0 matches
- `grep ".catch(() => {})" server/auth/routes.ts` returns 0 matches
- `grep "log.warn" server/ai.ts` includes "Failed to persist inference entry"
- `grep "log.warn" server/auth/routes.ts` includes "Failed to update user" and "Failed to clear lockout"
- Each catch handler includes `error: String(err)` in the log context object
  </acceptance_criteria>
  <done>All 7 empty catch handlers in ai.ts and auth/routes.ts replaced with warn-level logging that includes error string and relevant context (orgId, userId, email)</done>
</task>

<task type="auto">
  <name>Task 2: Replace empty catch handlers in routes and engine files</name>
  <files>server/hunt-engine.ts, server/outbox-processor.ts, server/routes/ai.ts, server/routes/shared.ts, server/routes/threat-hunting.ts, server/routes/war-room.ts</files>
  <read_first>server/hunt-engine.ts, server/outbox-processor.ts, server/routes/ai.ts, server/routes/shared.ts, server/routes/threat-hunting.ts, server/routes/war-room.ts, server/logger.ts</read_first>
  <action>
Replace every `.catch(() => {})` pattern with a `.catch((err) => log.warn("...", { error: String(err) }))` call. Ensure each file has a `const log = logger.child("module-name");` declaration (check if one already exists before adding).

In server/hunt-engine.ts (1 occurrence):
- Line 94: `await db.execute(sql\`SET statement_timeout = ...\`).catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to reset statement_timeout", { error: String(err) }))`

In server/outbox-processor.ts (2 occurrences):
- Line 112: `.catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to deliver outbox event", { error: String(err) }))`
- Line 161: `.catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to process outbox entry", { error: String(err) }))`

In server/routes/ai.ts (7 occurrences, all on `storage.incrementUsage(orgId, "ai_analyses")`):
- Lines 625, 727, 2305, 2357, 2405, 2562, 2638, 2753: Replace each `.catch(() => {})` with `.catch((err) => log.warn("Failed to increment AI usage", { error: String(err), orgId }))`

In server/routes/shared.ts (3 occurrences):
- Lines 252, 266, 279: Replace each `.catch(() => {})` with `.catch((err) => log.warn("Failed to dispatch webhook", { error: String(err) }))`

In server/routes/threat-hunting.ts (1 occurrence):
- Line 302: `.catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to record threat hunt activity", { error: String(err) }))`

In server/routes/war-room.ts (1 occurrence):
- Line 340: `.catch(() => {})` -- replace with `.catch((err) => log.warn("Failed to broadcast war room event", { error: String(err) }))`
  </action>
  <verify>
    <automated>cd "C:/Users/prath/OneDrive/Desktop/securenexus/.claude/worktrees/brave-vaughan" && grep -rn ".catch(() => {})" server/ --include="*.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
- `grep -rn ".catch(() => {})" server/ --include="*.ts"` returns 0 lines
- `grep "log.warn" server/routes/ai.ts` includes "Failed to increment AI usage"
- `grep "log.warn" server/outbox-processor.ts` includes "Failed to deliver outbox event"
- `grep "log.warn" server/routes/shared.ts` includes "Failed to dispatch webhook"
- Every replacement includes `error: String(err)` in the log context
  </acceptance_criteria>
  <done>All 15 remaining empty catch handlers replaced with warn-level logging. Zero `.catch(() => {})` patterns remain in the entire server/ directory.</done>
</task>

</tasks>

<verification>
Run: `grep -rn ".catch(() => {})" server/ --include="*.ts" | wc -l` -- must return 0
Run: `npx tsc --noEmit` -- must compile without new errors (logging changes should not break types)
</verification>

<success_criteria>
- Zero `.catch(() => {})` patterns remain in server/ directory
- All replacements use `log.warn()` with `error: String(err)` context
- TypeScript compilation passes without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-prerequisite-safety-fixes/01-01-SUMMARY.md`
</output>
